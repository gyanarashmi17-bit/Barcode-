# 🧾 Smart Expiry Management System — Frontend

This is the **React + TailwindCSS frontend** for the Smart Expiry Management System.

## 🚀 Features
- Modern animated blue gradient UI
- Product expiry input and tracking
- LocalStorage persistence
- Status detection (Fresh / Near Expiry / Expired)

## ⚙️ Setup Instructions
1. Install dependencies:
   ```bash
   npm install
   ```

2. Run development server:
   ```bash
   npm run dev
   ```

3. Access the app at:
   [http://localhost:5173](http://localhost:5173)
